name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Greeting
        run: echo "Hello World, Fernando!"
      - name: Goodbye
        run: echo "See ya later, Fefe"
